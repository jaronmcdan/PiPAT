# /etc/roi/roi.env (example)
# These are OPTIONAL. The current code reads config.py directly, but the provided
# systemd unit is set up to load this file so you can keep per-Pi settings outside
# the codebase if you later adopt env-based config.

# CAN
CAN_CHANNEL=can1
CAN_BITRATE=250000

# Serial multimeter
MULTI_METER_PATH=/dev/ttyUSB0
MULTI_METER_BAUD=38400

# VISA
ELOAD_VISA_ID=USB0::11975::34816::*::0::INSTR
AFG_VISA_ID=ASRL/dev/ttyACM0::INSTR

# GPIO / relay
K1_PIN_BCM=26
RELAY_ACTIVE_LOW=0
